# CHANGES — 2026-06-30

## Major Refactoring — Both Apps

Large-file refactoring pass across both codebases. No behavior changes — pure structural improvements for maintainability and readability.

---

## Customer App (Hetha_app)

### 1. subscription_page.dart — 3,016 → 1,257 lines (−58%)

The largest file in the project, split into focused modules:

| Extracted file | Lines | Responsibility |
|----------------|-------|----------------|
| `models/daily_order.dart` | 85 | DailyOrder data class (was defined inside the screen) |
| `screens/subscription/widgets/subscription_calendar.dart` | 270 | Calendar grid with day cells, legend, month navigation |
| `screens/subscription/widgets/subscription_status_card.dart` | 103 | Active/pending/cancelled status display |
| `screens/subscription/dialogs/pause_dialogs.dart` | 454 | Bulk pause date-range dialog + day management dialog |
| `screens/subscription/dialogs/cancellation_flow.dart` | 531 | Cancel subscription dialog (immediate/custom) + revert dialog |
| `screens/subscription/dialogs/subscription_action_sheets.dart` | 164 | Pause options sheet + edit options sheet |

The main file retains: state management, data loading, navigation logic, and the build scaffold.

---

### 2. subscription_day.dart — 1,825 → 1,346 lines (−26%)

| Extracted file | Lines | Responsibility |
|----------------|-------|----------------|
| `widgets/subscription_product_row.dart` | 147 | Product row tile with variant/price info and edit button |
| `widgets/quantity_selector_dialog.dart` | 166 | Quantity increment/decrement bottom sheet |
| `widgets/daily_order_summary.dart` | 174 | Order summary with items, totals, balance warning |

---

### 3. walletpage.dart — 1,324 → 772 lines (−42%)

| Extracted file | Lines | Responsibility |
|----------------|-------|----------------|
| `services/razorpay_payment_service.dart` | 96 | Reusable Razorpay wrapper (init, create order, open checkout, callbacks) |
| `screens/wallet/widgets/transaction_details_sheet.dart` | 404 | Transaction detail bottom sheet with item fetching |
| `screens/wallet/widgets/add_money_sheet.dart` | 124 | Add-money amount input bottom sheet |

**Note:** The new `RazorpayPaymentService` can also be adopted by `checkout.dart` to eliminate its duplicate Razorpay implementation.

---

## Admin Panel (Hetha_admin)

### 4. ad-hoc/page.tsx — 1,603 → 709 lines (−56%)

Extracted 6 inline modal components + shared types into `app/admin/ad-hoc/components/`:

- `types.ts` — Customer, Address, ModalProps interfaces
- `AddUserModal.tsx` — New ad-hoc user form
- `AddAddressModal.tsx` — Delivery address form
- `AddWalletModal.tsx` — Credit wallet form
- `CreateOrderModal.tsx` — One-time order with product search + delivery charge resolution
- `CreateSubModal.tsx` — Subscription creation with route/area resolution
- `ConvertToAppUserModal.tsx` — Promote ad-hoc user to Supabase Auth
- `index.ts` — Barrel re-exports

---

### 5. daily-ops/page.tsx — 921 → 262 lines (−72%)

Split the monolithic 3-tab dashboard into:

| Extracted file | Responsibility |
|----------------|----------------|
| `hooks/useDailyOps.ts` | All state + data fetching + mutations (date, orders, generate, finalize, reconcile, export) |
| `components/RunSheetTab.tsx` | Run sheet table with expandable order rows + inline editing |
| `components/LiveStatusTab.tsx` | Live delivery status view |
| `components/ReconciliationTab.tsx` | End-of-day reconciliation view |
| `types.ts` | Shared type definitions for daily ops |

---

### 6. services/dailyOpsService.ts — 756 → 37-line wrapper

Split the monolithic service into `services/dailyOps/`:

| Sub-module | Lines | Responsibility |
|------------|-------|----------------|
| `types.ts` | 74 | Shared interfaces |
| `generateRunSheet.ts` | 250 | The core run-sheet generation algorithm |
| `orders.ts` | 245 | Daily order CRUD, bulk ops, delivered-qty tracking |
| `runManagement.ts` | 94 | Finalize, reconcile, unlock, lock checking |
| `reporting.ts` | 98 | Live status, area/route summaries, print sheets, CSV export |
| `index.ts` | 35 | Barrel re-exports |

The original `dailyOpsService.ts` is now a thin re-export wrapper so existing imports don't break.

---

## Commits

- **Hetha_app:** `85660af` — `refactor: split large screen files into focused modules`
- **Hetha_admin:** `0b3ffe4` — `refactor: split large page files and service into focused modules`
